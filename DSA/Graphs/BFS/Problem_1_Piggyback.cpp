#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int b, e, p, n, m;
    cin >> b >> e >> p >> n >> m;

    

    while(m--)
    {
        int u, v;
        cin >> u >> v;


    }

    return 0;
}
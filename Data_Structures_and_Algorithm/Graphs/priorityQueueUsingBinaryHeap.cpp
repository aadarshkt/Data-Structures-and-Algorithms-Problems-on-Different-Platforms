#include <bits/stdc++.h>
using namespace std;


// C++ code to implement priority-queue
// using array implementation of
// binary heap

// The time complexity of all the operation is O(log N) except for GetMax() which has time complexity of O(1). 
 
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    

    return 0;
}
#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "debug.h"
#else
#define debug(...) 42
#endif

int fun(int a)
{
    cout << a << endl;

    int b = 5;

    cout << b << endl;

    cout << endl;

    return b;

    return b;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int a;
    cin >> a;
    int n=2;
    debug(a);

    for(int i=0; i<5; i++)
    {
        cout << a << endl;
    }    

    for(int i=0; i<n; i++)
    {
        cout << a << endl;
        cout << a << endl;

    }

    for(int i=0; i<6; i++)
    {
        cout << endl;
    }

        return 0;
}
#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;
    cin>>t;

    int n;
    cin>>n;

    int arr[n];

    for(int i=0; i<n; i++) cin>>arr[n];

    

    return 0;
}
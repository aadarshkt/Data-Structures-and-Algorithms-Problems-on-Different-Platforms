#include <bits/stdc++.h>
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    
    int arr[] = {6, 2, 5, 1, 7, 4, 8, 3};

    int n = sizeof(arr) / sizeof(arr[0]);

    return 0;
}
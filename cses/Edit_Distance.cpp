#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include <bits/dbg.h>
#else
#define dbg(...) 42
#endif

#define int long long

int32_t main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    string ns,ms;cin>>ns>>ms;
    int n=ns.length(),m=ms.length();
    int i=0,j=0,op=0;
    while(i<n&&j<m){
        if(ns[i]==ms[i]){
            i++,j++;
        }else{
            
        }
    }

    return 0;
}
#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include <bits/dbg.h>
#else
#define dbg(...) 42
#endif

#define int long long

int32_t main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while(t--){
        int n;cin>>n;
        string s1,s2,s3;cin>>s1>>s2>>s3;
        vector<int> pre1(2*n),pre2(2*n),pre3(2*n);
        int sum1=0,sum2=0,sum3=0;
        for(int i=0;i<2*n;i++){

        }
    }

    return 0;
}
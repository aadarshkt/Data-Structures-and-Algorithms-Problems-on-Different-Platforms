#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include <bits/dbg.h>
#else
#define dbg(...) 42
#endif

#define int long long

bool check(string &s){
    int n=s.length();
    map<int,int> mp;
    for(int i=0;i<n;i++){
        
    }
    return 1;
}

int sol(string &s,int t,int n){
    int len=s.length();
    if(len==n){
        bool ok=check(s);
        if(ok)return 1;
        return 0;
    }
}

int32_t main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int x,y;cin>>x>>y;


    return 0;
}